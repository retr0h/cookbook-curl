cookbook "cookbook-curl" do
  runtimes []
end

platform :debian do
  version "6.0.5" do
    box "debian-6.0.5"
    # Built Debian 6 with bento, and imported into vagrant.
    box_url "http://bogus.url"
  end
end
